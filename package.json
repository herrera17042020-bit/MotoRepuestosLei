{
  "name": "distribuidora-pos",
  "version": "1.0.0",
  "description": "Sistema de ventas e inventario para distribuidora de productos basicos",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "prisma generate",
    "dev": "nodemon server.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma db push",
    "prisma:studio": "prisma studio",
    "security:local": "node scripts/generar-seguridad-local.js",
    "cap:copy": "cap copy android",
    "cap:sync": "cap sync android",
    "cap:open": "cap open android",
    "test": "node --test"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/cli": "^8.5.2",
    "@capacitor/core": "^8.5.2",
    "@prisma/client": "5.22.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-validator": "^7.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7",
    "prisma": "5.22.0",
    "supertest": "^7.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
